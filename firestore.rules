rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is through the trusted Cloud Run API, including authenticated users.
    match /{document=**} { allow read, write: if false; }
  }
}
