## What this changes

<!-- A short description. Link the issue if there is one. -->

## How it was checked

- [ ] `npm run typecheck`, `npm test`, and `npm run build` pass locally
- [ ] Pages affected were checked at a phone width and a desktop width
- [ ] Copy changes were made in `data/seed.ts` (site copy lives in Firestore and is applied with `scripts/update-welcome.ts` after merge)

## Notes for reviewers

<!-- Anything a reviewer should look at closely: permissions, privacy, data handling, external links. -->
